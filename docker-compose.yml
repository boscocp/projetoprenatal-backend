version: '3.4'

services:
  backend:
    image: testedjango
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 1024:1024
  frontend:
    image: frontend
    build:
      context: frontend
      dockerfile: ./Dockerfile
    ports:
      - 4200:4200
    restart: "always"